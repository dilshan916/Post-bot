# Riddle Outro Card

Place your custom outro card image here (e.g. `outro.png`, `outro.jpg`).
The bot will automatically detect any image in this folder (e.g., .png, .jpg, .jpeg) and use it as the outro card overlay for Riddle Mode (4th mode).

If this folder is empty, the bot will fall back to the dynamically generated html/screenshot outro card.
